{
  "name": "react-best-practice",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm --filter react-app dev",
    "build": "pnpm --filter react-app build",
    "preview": "pnpm --filter react-app preview",
    "server": "json-server --watch db.json --delay 1000"
  }
}
